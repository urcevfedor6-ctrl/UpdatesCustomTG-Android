# CustomTG Android Updates

Create public repo `plugins-tg-custom/UpdatesCustomTG-Android`, then:

```bash
cd updates-repo-seed
git init -b main
git add .
git commit --trailer "Co-authored-by: Cursor <cursoragent@cursor.com>" -m "Seed latest.json"
git remote add origin https://github.com/plugins-tg-custom/UpdatesCustomTG-Android.git
git push -u origin main
```

Desktop updates remain in UpdatesCustomTG.
